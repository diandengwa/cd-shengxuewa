# 今日行动方案：点灯蛙K12升学AI产品启动清单
**日期**：生成于2026年5月
**原则**：你做决策+内容+关系；虾（OpenClaw指挥Claude/Codex）做代码+数据库+工程

---

## 先看这里：全天时间分配建议

```
上午（2-3小时）：你做 → 完成所有"今天必须你亲自做"的决策类任务
下午（你休息）：虾做 → 把工程任务清单喂给OpenClaw，让它跑起来
晚上（1小时）：你做 → 检查虾的输出，写明天需要的内容素材
```

---

# 第一部分：你做的任务（必须你亲自完成，AI替代不了）

## 🔴 T-YOU-1：写下产品基石文档（今天上午，60分钟）

拿一张A4纸或新建备忘录，手写以下5个问题的答案。
不要打开电脑查资料，就凭现在的认知写，写完就是你产品的"宪法"。

```
问题1：用一句话定义升学参谋（不超过20字）
  格式：为【谁】提供【什么】，解决【什么问题】
  示例（仅参考，不要照抄）："为成都K12家庭提供个性化、概率化的升学决策支持"

问题2：你永远不做的3件事（拒绝服务清单）
  参考：错题诊断、承诺入学结果、非成都家庭（需你自己确认）

问题3：你的目标用户画像（具体到一个人）
  写出一个真实的或虚构的家长：她叫什么、住哪个区、孩子几岁、
  她现在最焦虑的一句话是什么

问题4：Phase 1结束时（两周后），你期望看到的最小成果是什么
  不是功能列表，是一个场景："一个天府新区的妈妈，输入孩子情况后，
  能得到一份包含概率化建议的诊断卡片"

问题5：如果产品只能有一个核心功能，是什么
  （只能选一个，强迫自己选）
```

**完成后**：拍照，这就是你给虾的最高优先级上下文。

---

## 🔴 T-YOU-2：确认品牌与域名（今天上午，30分钟）

- [ ] 确认品牌名：**点灯蛙**保留，升级副标题为"成都K12升学参谋"
- [ ] 检查域名是否已有：`denglengwa.com` / `dengdengwa.com` / `ddw.ai`
      如果没有，今天在阿里云注册（约¥55/年）
- [ ] 确认微信公众号账号是否已注册，如没有立刻申请
      **注意**：服务号比订阅号更重要，要申请服务号（需营业执照）
- [ ] 决定：产品入口名称用"升学参谋"还是"点灯蛙升学"？
      这个名字会出现在微信菜单、对话界面的第一句话

---

## 🔴 T-YOU-3：竞品快照（今天上午，90分钟，不超时）

以下5个问题，每个问题用15分钟搜索，写3-5条结论即可，不要写报告。

```
问题1：打开"兰西小屋"最近10篇文章，记录：
  - 他们从不回答的问题类型是什么？（这是你的机会）
  - 家长在评论区最多抱怨的是什么？

问题2：搜索小红书"成都幼升小 2026"，记录：
  - 家长最困惑的3类问题（不是信息缺失，而是判断困难）
  - 有没有人提到"问了很多地方都得不到具体答案"

问题3：搜索知乎"成都小升初攻略"，记录：
  - 哪类问题下面的答案质量最差（说明性回答）？
  - 有没有人提到愿意付费获取个性化建议？

问题4：打开"升小派"小程序（如能找到），记录：
  - 它的核心功能是什么？
  - 用户会在哪个环节放弃？

问题5：搜索微信"成都升学"相关公众号，找3个，记录：
  - 各自最近一个月发了几篇文章？（判断更新频率）
  - 留言区有没有活跃互动？
```

**输出**：一份100字以内的竞品快照摘要，发给我，我帮你提炼内容策略。

---

## 🟡 T-YOU-4：¥29.9服务结构化记录表（今天，20分钟）

如果你现在已经在跑¥29.9的提分测试服务，建一个简单的表格开始记录。
以后每完成一单就填一行。

```
| 日期 | 孩子年级 | 学科 | 具体问题类型 | 家长最满意的部分 | AI初稿满足度(1-5) | 是否复购 |
|------|---------|------|------------|---------------|----------------|---------|
```

**不需要工具，微信文档/飞书表格/Excel都行，今天建好空表即可。**

---

## 🟡 T-YOU-5：找到3个种子用户（今天，随时可做）

不要发朋友圈，不要群发，就找3个你认识的成都家长，发私信：

```
参考话术（自然改写，不要照抄）：
"最近在做一个帮成都家长做升学规划的AI工具，
 还在内测阶段，你家孩子[学段]，
 有没有什么关于[幼升小/小升初/中考]特别搞不清楚的问题？
 我免费帮你用AI跑一次诊断，你给我反馈就行。"
```

**目标**：今天获得3个承诺参与内测的真实家长。
这3个人就是你Phase 1结束时的验收测试员。

---

## 🟡 T-YOU-6：写第一篇公众号文章的标题和大纲（今天晚上，30分钟）

不是今天发，但今天要把骨架写出来。

```
选一个你最熟悉的场景，写标题+5个段落标题：

推荐选题（选一个）：
A. "2026年成都幼升小：我用AI分析了100个家庭，发现这3类家长最容易踩坑"
B. "成都小升初摇号到底值不值得赌？我建了一个概率模型"
C. "为什么成都的升学攻略公众号，从来不告诉你最关键的那一步"

大纲格式（5个段落标题就够）：
1. 引子：一个真实的家长困境（你见过的）
2. 问题：为什么现有信息都没用
3. 框架：正确的决策思路是什么
4. 案例：用AI分析一个具体家庭情况（脱敏）
5. 行动：现在可以做的第一步
```

这篇文章会在Phase 2产品上线后发出，文章结尾跳转产品入口。

---

# 第二部分：虾做的任务（OpenClaw指挥Claude/Codex执行）

> **给虾的总指令**：
> "以下是今天的工程任务清单，按优先级顺序执行。
>  每完成一个模块，输出：①完成的代码/结构 ②遇到的问题 ③需要我确认的决策点。
>  所有数据库设计必须同时考虑升学诊断和未来提分产品线的复用需求。"

---

## 🔴 T-DEV-1：初始化项目结构（优先级最高，今天必须完成）

```
任务描述：
按照《完整实施方案v2.0》第三章的技术选型，
初始化完整的项目目录结构，并确认所有依赖可正常安装。

具体要做：
1. 创建项目根目录结构：
   dengdengwa/
   ├── backend/
   │   ├── main.py（FastAPI入口）
   │   ├── api/（路由）
   │   ├── core/（业务逻辑）
   │   ├── models/（数据模型）
   │   └── services/（外部服务）
   ├── data/
   │   ├── raw/
   │   ├── filtered/
   │   ├── extracted/
   │   └── integrated/
   ├── frontend/
   │   ├── index.html
   │   ├── chat.html
   │   └── static/
   └── scripts/（采集/提取脚本）

2. 安装并验证以下依赖：
   pip install fastapi uvicorn chromadb openai redis sqlalchemy aiohttp
   pip install scrapy yt-dlp requests beautifulsoup4
   pip install weasyprint apscheduler

3. 创建 requirements.txt

4. 验证：运行 uvicorn main:app，能看到FastAPI默认页面即为成功

验收标准：
  目录结构完整，uvicorn启动无报错，requirements.txt可复现环境
```

---

## 🔴 T-DEV-2：数据库完整初始化（今天必须完成）

```
任务描述：
创建所有SQLite表，字段设计必须同时满足升学诊断和未来提分产品线的需求。

需要创建的表（完整DDL）：

-- 家庭基础画像（两条产品线共用）
CREATE TABLE family_profiles (
    id TEXT PRIMARY KEY,
    user_id TEXT,
    hukou_district TEXT,
    residence_district TEXT,
    hukou_type TEXT,           -- 本市/随迁/跨区
    children JSON,             -- [{"age":6,"stage":"幼升小","school":null}]
    budget_range TEXT,
    has_property TEXT,
    property_district TEXT,
    special_needs JSON,

    -- 提分产品预留字段（现在就加，提分上线时直接有数据）
    subject_weakness JSON,     -- {"math":"几何","english":"写作"}
    recent_exam_scores JSON,   -- {"math":82,"english":78}
    tutoring_current TEXT,     -- 当前在用什么提分方式（调研用）
    grade_tracking_enabled INTEGER DEFAULT 0,

    profile_completeness REAL DEFAULT 0.0,
    is_pro INTEGER DEFAULT 0,
    created_at TEXT,
    updated_at TEXT,
    last_interaction_at TEXT,
    last_push_date TEXT,
    push_enabled INTEGER DEFAULT 1,
    push_frequency TEXT DEFAULT 'normal', -- dense/normal/key-only/off
    tier TEXT DEFAULT 'TIER_3'
);

-- 长期记忆日志（权威源，永不依赖外部框架）
CREATE TABLE family_memory_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id TEXT NOT NULL,
    memory_type TEXT,
    content TEXT NOT NULL,
    created_at TEXT NOT NULL,
    synced_to_openclaw INTEGER DEFAULT 0
);

-- 知识有效期管理
CREATE TABLE decision_frameworks (
    id TEXT PRIMARY KEY,
    scenario TEXT,
    condition_text TEXT,
    action_text TEXT,
    confidence REAL,
    source_author TEXT,
    source_date TEXT,
    valid_for_year INTEGER,
    policy_anchor TEXT,
    expiry_trigger TEXT,
    status TEXT DEFAULT 'active',
    bias_level TEXT DEFAULT 'low',
    bias_corrected INTEGER DEFAULT 0,
    created_at TEXT,
    last_verified_at TEXT,
    confidence_decay_rate REAL DEFAULT 0.05
);

-- 推送记录
CREATE TABLE push_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT NOT NULL,
    trigger_reason TEXT,
    message_preview TEXT,
    push_channel TEXT DEFAULT 'wechat',
    sent_at TEXT,
    opened_at TEXT,
    converted INTEGER DEFAULT 0
);

-- ¥29.9提分服务记录（从今天开始结构化记录）
CREATE TABLE tutoring_sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id TEXT,
    child_grade TEXT,
    subject TEXT,
    problem_type TEXT,
    ai_draft_rating INTEGER,   -- 1-5分：AI初稿满足家长需求的程度
    parent_satisfied_part TEXT,
    price REAL DEFAULT 29.9,
    repurchased INTEGER DEFAULT 0,
    created_at TEXT
);

-- 索引
CREATE INDEX idx_profile_user ON family_profiles(user_id);
CREATE INDEX idx_memory_session ON family_memory_log(session_id);
CREATE INDEX idx_memory_synced ON family_memory_log(synced_to_openclaw);
CREATE INDEX idx_push_user ON push_log(user_id);

验收标准：
  db.py 中所有表创建无报错，sqlite3 命令行可查到所有表
```

---

## 🔴 T-DEV-3：ChromaDB初始化（今天完成）

```
任务描述：
创建所有ChromaDB collection，并写入3条测试数据验证可用性。

需要创建的collections：
1. policy_rag         → P0-P1级官方政策文件
2. competitor_facts   → 竞品事实声明（含有效期字段）
3. competitor_frameworks → 竞品决策框架（含偏见修正）
4. pain_points        → 家长焦虑图谱

每个collection的metadata schema：
{
  "source_author": str,
  "source_date": str,       # YYYY-MM格式
  "valid_for_year": int,
  "status": str,            # active/expired/pending_review
  "confidence": float,
  "bias_level": str,        # low/medium/high
  "stage": str,             # 幼升小/小升初/初升高/高考
  "district": str           # 适用区域
}

写入测试数据：
  向policy_rag写入1条测试政策摘要
  向competitor_facts写入1条测试事实声明
  执行一次query，验证召回正常

验收标准：
  chroma_init.py 运行无报错
  test_query.py 能返回召回结果
```

---

## 🔴 T-DEV-4：偏见过滤器实现（今天完成，这是所有提取器的前置）

```
任务描述：
实现BIAS_FILTER_PROMPT，并用3篇测试文章验证效果。

参考方案文档《完整实施方案v2.0》第一章 1.2节 提取器0的完整实现。

要求：
1. 输入：原始文章文本（字符串）
2. 输出：JSON格式
   {
     "bias_level": "low|medium|high",
     "bias_types": ["commercial_tie", "anxiety_amplification", "outdated"],
     "bias_details": "具体说明",
     "usable_parts": "尽管有偏见，以下部分仍可提取：...",
     "extraction_instruction": "提取时需要修正的方向"
   }
3. 错误处理：LLM调用失败时返回 {"bias_level": "medium", "error": true}
4. 写入日志：每次分析结果存入 data/filtered/bias_reports/ 目录

测试要求：
  准备3篇测试文章（可从现有竞品文章中选取）：
  - 1篇明显有商业利益绑定的
  - 1篇相对客观的
  - 1篇时效性存疑的（2022年的旧文章）
  运行后人工验证分类是否合理

验收标准：
  3篇文章分类结果合理（不需要完美，需要方向正确）
  JSON格式正确，无解析错误
```

---

## 🟡 T-DEV-5：基础对话接口（今天完成骨架，明天完善）

```
任务描述：
实现 /api/chat 的基础骨架，能跑通完整链路即可，不需要最优效果。

链路：接收消息 → 加载画像（空画像兜底）→ 简单RAG召回 → 调LLM → 流式返回

实现要求：
1. FastAPI SSE流式端点：
   POST /api/chat
   接收：{"session_id": str, "message": str, "history": list}
   返回：text/event-stream

2. 简单家庭画像加载：
   根据session_id查family_profiles表
   如果没有：返回空画像，AI第一句话是引导收集信息

3. 接入DeepSeek API：
   使用 DEEPSEEK_API_KEY 环境变量
   模型：deepseek-chat
   实现流式输出

4. 基础System Prompt硬编码（从方案文档2.1节复制）

5. 健康检查接口：
   GET /health → {"status": "ok", "timestamp": ...}

不需要做的（留给明天）：
  - 双路RAG召回（今天用单路即可）
  - 记忆系统集成
  - 诊断卡片生成

验收标准：
  curl -X POST /api/chat -d '{"session_id":"test","message":"你好","history":[]}'
  能看到流式文字输出，不报错
```

---

## 🟡 T-DEV-6：管理后台（手动录入通道，今天完成简单版）

```
任务描述：
实现一个极简的管理后台页面，允许手动粘贴竞品文章触发提取流程。
这是防止爬虫失效的安全网，必须今天就能用。

功能要求（最简实现）：
  一个HTML表单，字段：
  - 来源作者（文本框）
  - 来源日期（日期选择）
  - 内容类型（下拉：公众号文章/知乎回答/论坛帖子/视频转写）
  - 文章内容（大文本框，粘贴用）
  - 提交按钮

后端接口：
  POST /admin/ingest
  接收表单数据
  → 调用偏见过滤器（T-DEV-4）
  → 存入 data/raw/articles/ 目录
  → 返回："已接收，正在处理（偏见等级：XX）"

不需要登录验证（内部使用，加个简单密码参数即可）

验收标准：
  粘贴一篇文章，点提交，在data/raw/articles/下能看到新文件
  页面返回偏见等级
```

---

## 🟡 T-DEV-7：知识有效期维护脚本（今天写好，配置定时任务）

```
任务描述：
实现 KnowledgeExpiryManager，并配置每周日凌晨2点运行。

参考方案文档《完整实施方案v2.0》第一章 1.3节完整代码。

核心功能：
1. _check_annual_policy_expiry()
   每年4-8月将上一年policy_anchor的记录标记为pending_review

2. _apply_confidence_decay()
   超过90天未验证的知识，confidence -= confidence_decay_rate

3. _generate_expiry_report()
   输出JSON报告：本周新增N条、过期N条、待审核N条
   存入 data/reports/weekly_expiry/ 目录

APScheduler配置：
  from apscheduler.schedulers.asyncio import AsyncIOScheduler
  scheduler.add_job(expiry_manager.run_weekly_check, "cron",
                    day_of_week="sun", hour=2)

验收标准：
  手动调用 run_weekly_check()，无报错
  生成一份有效期报告文件
```

---

## ⚪ T-DEV-8：前端对话界面（今天做基础版，不追求美观）

```
任务描述：
实现最简单的对话界面HTML页面，能跑通对话即可。

要求：
1. 单文件HTML（不需要框架）
2. 包含：消息输入框 + 发送按钮 + 对话历史展示区
3. 接入 /api/chat SSE流式接口
4. 显示流式输出（字符逐渐出现，不是一次性返回）
5. 支持Enter键发送

不需要做的：
  - 诊断卡片渲染（留给Phase 2）
  - 微信OAuth登录（留给Phase 2）
  - 任何美化设计（先跑通功能）

验收标准：
  在浏览器打开，输入一句话，能看到AI流式回复
```

---

# 第三部分：今天结束前的验收清单

```
你的任务（5件）：
□ T-YOU-1：产品基石文档写完（拍照存档）
□ T-YOU-2：域名/公众号确认（至少完成域名注册）
□ T-YOU-3：竞品快照完成（100字摘要）
□ T-YOU-4：提分记录表建好（空表即可）
□ T-YOU-5：找到3个种子用户（获得承诺即可）

虾的任务（8件）：
□ T-DEV-1：项目结构初始化 + uvicorn启动成功
□ T-DEV-2：SQLite所有表创建成功
□ T-DEV-3：ChromaDB初始化 + 测试召回成功
□ T-DEV-4：偏见过滤器可运行，3篇测试文章分类合理
□ T-DEV-5：/api/chat 流式输出可跑通
□ T-DEV-6：管理后台可粘贴文章并触发处理
□ T-DEV-7：有效期维护脚本可手动调用
□ T-DEV-8：前端页面可对话（不要求美观）
```

---

# 第四部分：明天开始的任务预告（不要今天做）

```
明天你做：
  - 把3个种子用户约过来测试（或线上1v1视频）
  - 根据测试反馈调整System Prompt
  - 开始写第一篇公众号文章

明天虾做：
  - 双路RAG完整实现（政策RAG + 竞品知识库两路召回）
  - 冲突裁判机制（P0-P4权威排序）
  - 家庭画像3轮对话自动建立逻辑
  - 诊断卡片HTML生成（对话结束后插入）
  - 首批竞品内容采集（兰西论坛100帖 + 知乎50篇）
```

---

# 附：给虾的总指令模板（直接复制发给OpenClaw）

```
你是这个项目的首席工程师，今天需要完成以下8个开发任务（T-DEV-1到T-DEV-8）。

项目背景：成都K12升学AI产品，品牌名"点灯蛙"，技术栈：
  后端：FastAPI + SQLite + ChromaDB + Redis + DeepSeek API
  前端：原生HTML + JS（暂不用框架）
  部署环境：Ubuntu 24，4核8G，60G硬盘

执行规则：
  1. 按T-DEV-1→T-DEV-8的顺序执行，前一个验收通过再做下一个
  2. 每完成一个任务，输出：
     ① 完成的核心代码片段
     ② 验收测试命令
     ③ 遇到的决策点（需要我确认的）
  3. 所有数据库字段必须同时考虑升学诊断和提分产品线的复用
  4. 所有外部API调用（DeepSeek等）必须有超时和异常捕获
  5. 不要过度设计，今天的目标是"能跑通"，不是"最优"

参考文档：《完整实施方案v2.0》（已发给你）

第一个任务是T-DEV-1，开始。
```
